namespace SIL.Machine.Translation
{
    public enum WordAlignmentDirection
    {
        // source -> target
        Direct,

        // target -> source
        Inverse,

        // source <-> target
        Symmetric
    }
}
