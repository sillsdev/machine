apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: machine-api-server
  name: machine-api-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: machine-api-server
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: machine-api-server
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - command:
            - dotnet
            - /app/api_server/SIL.Machine.WebApi.ApiServer.dll
          workingDir: /app/api_server
          env:
            - name: ASPNETCORE_Kestrel__Endpoints__Https__Url
              value: http://*
            - name: ASPNETCORE_ConnectionStrings__Hangfire
              value: mongodb://mongo:27017/machine_jobs
            - name: ASPNETCORE_ConnectionStrings__Mongo
              value: mongodb://mongo:27017/machine
            - name: ASPNETCORE_ConnectionStrings__EngineServer
              value: http://machine-engine-server:81
            - name: ASPNETCORE_Logging__LogLevel__Default
              value: Information
            - name: ASPNETCORE_Logging__LogLevel__Microsoft.AspNetCore
              value: Warning
              {{- if not .Values.localVolume}}
              {{- template "secrets.clearml" }}
              {{- template "secrets.s3" }}
              {{- end}}
          image: ghcr.io/sillsdev/machine:latest
          imagePullPolicy: "Always"
          name: machine-api-server
          ports:
            - containerPort: 80
          resources:
            limits:
              memory: "1000Mi"
              cpu: "500m"
            requests:
              memory: "100Mi"
              cpu: "100m"
          volumeMounts:
            - mountPath: /var/lib/machine/corpora
              name: machine-api-server-corpora
            - mountPath: /root/.aspnet/DataProtection-Keys
              name: machine-api-server-keys
      hostname: machine-api-server
      restartPolicy: Always
      {{- if .Values.localVolume }}
      volumes:
        - hostPath:
            path: /host/corpora
          name: machine-api-server-corpora
        - hostPath:
            path: /host/DataProtection-Keys
          name: machine-api-server-keys
      {{- else}}
      volumes:
        - name: machine-api-server-corpora
          persistentVolumeClaim:
            claimName: machine-corpora-claim
        - name: machine-api-server-keys
          persistentVolumeClaim:
            claimName: machine-keys-claim
      {{- end}}
status: {}
