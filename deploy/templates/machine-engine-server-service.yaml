apiVersion: v1
kind: Service
metadata:
  labels:
    app: machine-server
  name: machine-server
spec:
  ports:
    - name: "80"
      port: 80
      targetPort: 80
  selector:
    app: machine-server
status:
  loadBalancer: {}
